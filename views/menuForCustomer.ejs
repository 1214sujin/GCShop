  <nav class="navbar navbar-expand-lg bg-body-tertiary bg-dark border-bottom border-body fixed-top" data-bs-theme="dark">
    <div class="container-fluid">
      <a class="navbar-brand" href="/shop/all">Gachon</a>
      
      <div class="collapse navbar-collapse" id="navbarSupportedContent">
        <!-- 메뉴 시작 -->
        <ul class="navbar-nav me-auto mb-2 mb-lg-0">
          <li class="nav-item">
            <a class="nav-link active" aria-current="page" href="/shop/all">Home</a>
          </li>
          <% for (let i=0; i<categories.length; i++) { %>
          <li class="nav-item">
            <a class="nav-link" href="/shop/<%= categories[i].sub_id %>"><%= categories[i].sub_name %></a>
          </li>
          <% } %>
          <li class="nav-item dropdown">
            <a class="nav-link dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">
              게시판
            </a>
            <ul class="dropdown-menu">
              <% for (let i=0; i<boardtypes.length; i++) { %>
                <li><a class="dropdown-item" href="/board/view/<%= boardtypes[i].type_id %>/1"><%= boardtypes[i].title %></a></li>
              <% } %>
            </ul>
          </li>
        </ul>
        <!-- 메뉴 끝 -->

        <!-- 검색창 시작 -->
        <form class="d-flex" role="search">
          <input class="form-control me-2" type="search" placeholder="Search" aria-label="Search">
          <button class="btn btn-outline-success" type="submit">Search</button>
        </form>
        <!-- 검색창 끝 -->

        <!-- 로그인 -->
        <ul class="me-auto mb-2 mb-lg-0">
          <li class="nav-item dropdown">
            <a class="nav-link dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">
              <button type="button"  data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
              </button>
            </a>
            <ul class="dropdown-menu">
              
              <% if (locals.logined) { %>
                <li><a class="dropdown-item" href="/auth/login">로그인</a></li>
                <li><a class="dropdown-item" href="/auth/signup">회원가입</a></li>
                <li><a class="dropdown-item" href="#">아이디/비번찾기</a></li>
              <% } else { %>
                <li><a class="dropdown-item" href="/auth/logout_process" onclick="return confirm('로그아웃하시겠습니까?')">로그아웃</a></li>
              <% } %>
              
            </ul>
          </li>
        </ul>
        <!-- 로그인 끝 -->

      </div>

      <nav class="navbar bg-body-tertiary">
        <button class="btn btn-sm btn-outline-warning me-2" type="button">장바구니</button>
        <% if (!locals.logined) { %>
          <button class="btn btn-sm btn-outline-warning me-2" type="button">구매내역</button>
        <% } %>
      </nav>

      <nav class="navbar" >
        <div class="container-fluid text-white"  ><%=who%></div>
      </nav>

    </div>
  </nav>  